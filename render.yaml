services:
  - type: web
    name: agente-financiero
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python startup_cloud.py
    envVars:
      - key: OPENROUTER_KEY
        sync: false
      - key: RENDER
        value: "true"
